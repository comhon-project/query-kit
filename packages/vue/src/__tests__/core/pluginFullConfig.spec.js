import { describe, it, expect } from 'vitest'

import plugin  from '../../core/Plugin';
import { locale }  from '../../i18n/i18n';

describe('plugin', () => {
  it('full config', () => {
    // TODO
  });
})